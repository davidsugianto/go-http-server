name: Pipeline

on:
  push:
    branches:
      - master
      - main
      - 'release/**'
    tags:
      - 'v*.*.*'
  pull_request:
    branches:
      - master
      - main

jobs:
  build:
    uses: davidsugianto/shared-workflows/.github/workflows/go-docker-ci.yaml@master
    with:
      go-version: "1.25"
      image-name: docker.io/${{ github.repository }}
      run-tests: false
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  deploy-staging:
    needs: build
    if: startsWith(github.ref, 'refs/heads/release/')
    uses: davidsugianto/shared-workflows/.github/workflows/kustomize-image-tag.yaml@master
    with:
      gitops-repo: davidsugianto/idp-gitops-manifests
      image: docker.io/${{ github.repository }}
      tag: staging-latest
      kustomize-path: environments/staging/go-http-server
      create-pr: true
    secrets:
      git-token: ${{ secrets.PAT_TOKEN }}

  deploy-prod:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    uses: davidsugianto/shared-workflows/.github/workflows/kustomize-image-tag.yaml@master
    with:
      gitops-repo: davidsugianto/idp-gitops-manifests
      image: docker.io/${{ github.repository }}
      tag: ${{ github.ref_name }}
      kustomize-path: environments/prod/go-http-server
      create-pr: true
    secrets:
      git-token: ${{ secrets.PAT_TOKEN }}
